import React from 'react';
import propTypes from 'prop-types';

export function RecipeFormContainer(props) {
    return (
        <div>RecipeFormContainer</div>
    );
}

RecipeFormContainer.propTypes = {

};
