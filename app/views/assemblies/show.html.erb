<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @assembly.name %>
</p>

<p>
  <strong>Items:</strong>
    <ul>
        <% @assembly.items.each do |item| %>
            <li><%= item.name %> - <%= item.department.name %></li>
        <% end %>
    </ul>
</p>

<%= link_to 'Edit', edit_assembly_path(@assembly) %> |
<%= link_to 'Back', assemblies_path %>
